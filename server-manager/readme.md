# Server Manager

An express web app which runs on the ec2 instance running the minecraft server, responsible for managing the mc server directly. Primarily handles shutting down and backing up the world.
